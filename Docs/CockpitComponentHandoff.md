# Component assignment and handoff template

Copy this template into the assigned component directory as `HANDOFF.md`. Fill assignment fields before work and delivery fields before review. Use `unknown`, `not tested` or `not applicable` instead of leaving ambiguous blanks.

## Assignment

- Component and worker task:
- Machine:
- Branch and exact starting commit:
- Owned directory:
- Contract version and accepted dependency commits:
- Deliverable files:
- Acceptance criteria:
- Dimensions or datums that must be resolved before modeling:

## Delivered revision

- Exact component commit:
- Blender version and required add-ons:
- Source `.blend`, scripts and textures:
- Export file and SHA-256:
- Export settings:
- Review images:
- External dependencies and relative-path checks:

## Evidence and behavior

- Source IDs, manual pages and image IDs:
- Apollo 11 evidence versus later-mission substitutions:
- Measured dimensions versus inferred/provisional dimensions:
- Moving parts, pivot axes, neutral poses and limits:
- Guard, detent and spring-return behavior:
- Named display regions and mounting datums:
- Runtime owner for each control: AGC, LMCore, another subsystem, or unimplemented:
- Simulation binding status: visual only, mechanical demonstration, or tested runtime connection:

## Validation

- Geometry, normals, hierarchy, units and names:
- Clean reopen and dependency resolution:
- USDZ export/import checks:
- Reference comparison:
- Runtime and hardware tests actually performed:
- Render time, peak memory and machine:
- Known gaps, contradictions and contract-change proposals:

## Integration acceptance

Coordinator fills this after review:

- Accepted component commit:
- Integration commit:
- Acceptance evidence:
- Follow-up work:
